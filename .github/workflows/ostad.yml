name: CICD pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  print_the_file:
    runs-on : ubuntu-latest

    steps:
      - name: Git Clone
        uses: actions/checkout@v2

      - name: Print The print 
        run: |
          cat hello.txt
          ls 
          pwd















